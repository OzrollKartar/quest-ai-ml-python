requests>=2.31
numpy>=1.24
pymongo>=4.6          # only needed for the MongoDB store (--store mongo)
# Generation runs on Ollama (external): `ollama pull gemma3:1b` (or gemma3:4b)
# Optional semantic retrieval:          `ollama pull nomic-embed-text`
